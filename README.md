# 本地双击版

这个文件夹是可以离线/本地试用的版本。

使用方式：

1. 双击 `index.html` 直接打开。
2. 如果浏览器因为本地文件限制导致音乐、配置或照片没有加载，双击 `start-birthdaycake.bat`，它会在本机启动一个临时网页服务并自动打开。

需要保持在同一个文件夹里的内容：

- `index.html`
- `app.js`
- `styles.css`
- `image/`
- `bgm/`
- `config/`

